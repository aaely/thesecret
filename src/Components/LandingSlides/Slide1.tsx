import { useTransition, animated, useTrail } from "react-spring"

const Slide1: Function = (props: any) => {

    //const trail = useTrail()
    return(
        <>
            <h1 style={{textAlign: 'center', marginTop: '10%'}}>Welcome to My Page</h1>
        </>
    )
}

export default Slide1