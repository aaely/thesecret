import { useState } from 'react'
import { useTransition, animated } from 'react-spring'

const FrontEndOpen: Function = () => {

    return(
        <>
            <h3 style={{textAlign: 'center', marginTop: '3%'}}>State Management</h3>
            <h3 style={{textAlign: 'center'}}>Animations</h3>
            <h3 style={{textAlign: 'center'}}>API Calls</h3>
        </>
    )
}

export default FrontEndOpen